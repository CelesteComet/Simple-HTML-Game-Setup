# Simple-HTML-Game-Setup
A simple skeleton to produce a simple HTML game.

# Installation
1. Make sure to have NPM installed.
2. Within directory, run "npm install".
3. Run "npm start".
4. Navigate to localhost:5000

